/// @description Insert description here
// You can write your code in this editor

//find their y, determine whether my y is above or below within a certain range. If it's above, run a vertical collision. If it's below, it'll be a horizontal collision.

//Enemy collision
/*
if instance_exists(obj_Enemy) && place_meeting(x,y, obj_Enemy){
	
	if obj_Enemy.y - (sprite_get_height(obj_Enemy))/2 < y{
		hspeed = 0;
		if obj_Enemy.x < x
		{
			x = obj_Enemy.x + (sprite_get_width(obj_Enemy.sprite_index)/2 + sprite_get_width(sprite_index)/2);
		}
		if obj_Enemy.x > x
		{
	 
			x = obj_Enemy.x - (sprite_get_width(obj_Enemy.sprite_index)/2 + sprite_get_width(sprite_index)/2);
		}
	}
	else if obj_Enemy.y - (sprite_get_height(obj_Enemy))/2 > y && vspeed > 0
	{
		y = obj_Enemy.y - (sprite_get_width(obj_Enemy.sprite_index)/2 + sprite_get_width(sprite_index)/2);
		vspeed -= 100;
	}
	
}

*/