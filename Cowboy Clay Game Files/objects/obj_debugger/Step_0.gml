//var _maxpad = gamepad_get_device_count();

//for (var i = 0; i < _maxpad; i++) {
//	if gamepad_is_connected(i) {
//		show_debug_message("Gamepad " + string(i) + " reads as " + gamepad_get_description(i));

//		for(var j = 0; j < gamepad_axis_count(i); j++) {
//			if abs( gamepad_axis_value(i,j)) > 0.5 show_debug_message("Button " + string(j) + " is being pressed.");
//		}
//	}
//}
